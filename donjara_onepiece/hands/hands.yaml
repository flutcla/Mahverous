大集結セット:
  構造:
    9
  制約:
    - 同色(a, b, c)
    - 同色(d, e, f)
    - 同色(g, h, i)
    - a.色 == d.色 == g.色

麦わらの一味セット:
  構造:
    6 3
  制約:
    - 同色(a, b, c)
    - 同色(d, e, f)
    - a.色 == d.色 == '赤'
    - 同色(g, h, i) or 特殊役(g, h, i)

最悪の世代セット:
  構造:
    6 3
  制約:
    - 同色(a, b, c)
    - 同色(d, e, f)
    - a.色 == d.色 == '緑'
    - 同色(g, h, i) or 特殊役(g, h, i)

四皇セット:
  構造:
    6 3
  制約:
    - 同色(a, b, c)
    - 同色(d, e, f)
    - a.色 == d.色 == '紫'
    - 同色(g, h, i) or 特殊役(g, h, i)

海軍セット:
  構造:
    6 3
  制約:
    - 同色(a, b, c)
    - 同色(d, e, f)
    - a.色 == d.色 == '水'
    - 同色(g, h, i) or 特殊役(g, h, i)

革命軍セット:
  構造:
    6 3
  制約:
    - 同色(a, b, c)
    - 同色(d, e, f)
    - a.色 == d.色 == '白'
    - 同色(g, h, i) or 特殊役(g, h, i)

新世界の海賊たちセット:
  構造:
    3 3 3
  制約:
    - 同色(a, b, c)
    - a.色 == '緑'
    - 同色(d, e, f)
    - d.色 == '紫'
    - (同色(g, h, i) and g.色 == '赤') or 特殊役(g, h, i)

激突する正義セット:
  構造:
    3 3 3
  制約:
    - 同色(a, b, c)
    - a.色 == '緑'
    - 同色(d, e, f)
    - d.色 == '紫'
    - (同色(g, h, i) and g.色 == '赤') or 特殊役(g, h, i)

特殊役2個セット:
  構造:
    3 3 3
  制約:
    - 同色(a, b, c)
    - 特殊役(d, e, f)
    - 特殊役(g, h, i)

特殊役3個セット:
  構造:
    3 3 3
  制約:
    - 特殊役(a, b, c)
    - 特殊役(d, e, f)
    - 特殊役(g, h, i)
