大集結セット:
  構造: 9
  前処理:
    - Rule().点数 = 45
    - Rule().内訳 = ['大集結セット']
  制約:
    - and
    -
      - 同色(a, b, c)
      - 同色(d, e, f)
      - 同色(g, h, i)
      - a.色 == d.色 == g.色
  後処理:
    - Rule().成立役一覧.append((Rule().点数, Rule().内訳))

麦わらの一味セット:
  構造: 6 3
  前処理:
    - Rule().点数 = 21
    - Rule().内訳 = ['麦わらの一味セット']
  制約:
    - and
    -
      - 同色(a, b, c)
      - 同色(d, e, f)
      - a.色 == d.色 == '赤'
      - 同色(g, h, i) or 特殊役(g, h, i)
  後処理:
    - Rule().成立役一覧.append((Rule().点数, Rule().内訳))

最悪の世代セット:
  構造: 6 3
  前処理:
    - Rule().点数 = 24
    - Rule().内訳 = ['最悪の世代セット']
  制約:
    - and
    -
      - 同色(a, b, c)
      - 同色(d, e, f)
      - a.色 == d.色 == '緑'
      - 同色(g, h, i) or 特殊役(g, h, i)
  後処理:
    - Rule().成立役一覧.append((Rule().点数, Rule().内訳))

四皇セット:
  構造: 6 3
  前処理:
    - Rule().点数 = 27
    - Rule().内訳 = ['四皇セット']
  制約:
    - and
    -
      - 同色(a, b, c)
      - 同色(d, e, f)
      - a.色 == d.色 == '紫'
      - 同色(g, h, i) or 特殊役(g, h, i)
  後処理:
    - Rule().成立役一覧.append((Rule().点数, Rule().内訳))

海軍セット:
  構造: 6 3
  前処理:
    - Rule().点数 = 24
    - Rule().内訳 = ['海軍セット']
  制約:
    - and
    -
      - 同色(a, b, c)
      - 同色(d, e, f)
      - a.色 == d.色 == '水'
      - 同色(g, h, i) or 特殊役(g, h, i)
  後処理:
    - Rule().成立役一覧.append((Rule().点数, Rule().内訳))

革命軍セット:
  構造: 6 3
  前処理:
    - Rule().点数 = 30
    - Rule().内訳 = ['革命軍セット']
  制約:
    - and
    -
      - 同色(a, b, c)
      - 同色(d, e, f)
      - a.色 == d.色 == '白'
      - 同色(g, h, i) or 特殊役(g, h, i)
  後処理:
    - Rule().成立役一覧.append((Rule().点数, Rule().内訳))

新世界の海賊たちセット:
  構造: 3 3 3
  前処理:
    - Rule().点数 = 12
    - Rule().内訳 = ['新世界の海賊たちセット']
  制約:
    - and
    -
      - 同色(a, b, c)
      - a.色 == '緑'
      - 同色(d, e, f)
      - d.色 == '紫'
      - (同色(g, h, i) and g.色 == '赤') or 特殊役(g, h, i)
  後処理:
    - Rule().成立役一覧.append((Rule().点数, Rule().内訳))

激突する正義セット:
  構造: 3 3 3
  前処理:
    - Rule().点数 = 15
    - Rule().内訳 = ['激突する正義セット']
  制約:
    - and
    -
      - 同色(a, b, c)
      - a.色 == '水'
      - 同色(d, e, f)
      - d.色 == '白'
      - (同色(g, h, i) and g.色 == '赤') or 特殊役(g, h, i)
  後処理:
    - Rule().成立役一覧.append((Rule().点数, Rule().内訳))

特殊役3個セット:
  構造: 3 3 3
  前処理:
    - Rule().点数 = 3
    - Rule().内訳 = ['特殊役3個セット']
  制約:
    - and
    -
      - 特殊役(a, b, c)
      - 特殊役(d, e, f)
      - 特殊役(g, h, i)
  後処理:
    - Rule().成立役一覧.append((Rule().点数, Rule().内訳))

特殊役2個セット:
  構造: 3 3 3
  前処理:
    - Rule().点数 = 3
    - Rule().内訳 = ['特殊役2個セット']
  制約:
    - and
    -
      - 同色(a, b, c)
      - 特殊役(d, e, f)
      - 特殊役(g, h, i)
  後処理:
    - Rule().成立役一覧.append((Rule().点数, Rule().内訳))
