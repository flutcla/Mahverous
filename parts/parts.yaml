雀頭 s t:
  順番: False
  制約:
    s == t

刻子 a b c:
  順番: False
  制約:
    a == b and
    b == c

順子 a b c:
  順番: True
  制約:
    a.種別 == '数牌' and
    b.種別 == '数牌' and
    c.種別 == '数牌' and
    a.柄 == b.柄 and
    b.柄 == c.柄 and
    a.数値 + 1 == b.数値 and
    b.数値 + 1 == c.数値
