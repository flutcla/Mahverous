ミラクルセット:
  構造:
    9
  制約:
    - 同色(a, b, c)
    - 同色(d, e, f)
    - 同色(g, h, i)
    - a.色 == d.色 == g.色

2個1個セット:
  構造:
    6 3
  制約:
    - 同色(a, b, c)
    - 同色(d, e, f)
    - a.色 == d.色
    - 同色(g, h, i)

いろいろセット:
  構造:
    3 3 3
  制約:
    - 同色(a, b, c)
    - 同色(d, e, f)
    - 同色(g, h, i)

大集合セット:
  構造:
    9
  制約:
    - len(set(x.色 for x in [a, b, c, d, e, f, g, h, i])) == 9

空の旅セット:
  構造:
    3 3 3
  制約:
    - タケコプター同色(a, b, c)
    - タケコプター同色(d, e, f)
    - タケコプター同色(g, h, i)

野比家セット:
  構造:
    3 3 3
  制約:
    - a == のび太 or a == のび太T
    - b == のび太 or b == のび太T
    - c == のび太 or c == のび太T
    - d == e == f == のび太のママ
    - g == h == i == のび太のパパ
