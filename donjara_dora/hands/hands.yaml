ミラクルセット:
  構造: 9
  前処理:
    - Rule().点数 = 600
  制約:
    - and
    -
      - 同色(a, b, c)
      - 同色(d, e, f)
      - 同色(g, h, i)
      - a.色 == d.色 == g.色
  後処理:
    - Rule().成立役一覧.append((Rule().点数, 'ミラクルセット'))

2個1個セット:
  構造: 6 3
  前処理:
    - Rule().点数 = 300
  制約:
    - and
    -
      - 同色(a, b, c)
      - 同色(d, e, f)
      - a.色 == d.色
      - 同色(g, h, i)
  後処理:
    - Rule().成立役一覧.append((Rule().点数, '2個1個セット'))

いろいろセット:
  構造: 3 3 3
  前処理:
    - Rule().点数 = 120
  制約:
    - and
    -
      - 同色(a, b, c)
      - 同色(d, e, f)
      - 同色(g, h, i)
  後処理:
    - Rule().成立役一覧.append((Rule().点数, 'いろいろセット'))

大集合セット:
  構造: 9
  前処理:
    - Rule().点数 = 60
  制約:
    - len(set(x.色 for x in [a, b, c, d, e, f, g, h, i])) == 9
  後処理:
    - Rule().成立役一覧.append((Rule().点数, '大集合セット'))

空の旅セット:
  構造: 3 3 3
  前処理:
    - Rule().点数 = 600
  制約:
    - and
    -
      - タケコプター同色(a, b, c)
      - タケコプター同色(d, e, f)
      - タケコプター同色(g, h, i)
  後処理:
    - Rule().成立役一覧.append((Rule().点数, '空の旅セット'))

野比家セット:
  構造: 3 3 3
  前処理:
    - Rule().点数 = 420
  制約:
    - and
    -
      - 同色(a, b, c)
      - a.色 == '黄'
      - d == e == f == のび太のママ
      - g == h == i == のび太のパパ
  後処理:
    - Rule().成立役一覧.append((Rule().点数, '野比家セット'))

剛田家セット:
  構造: 3 3 3
  前処理:
    - Rule().点数 = 420
  制約:
    - and
    -
      - 同色(a, b, c)
      - a.色 == '水'
      - d == e == f == ジャイアンのかあちゃん
      - g == h == i == ジャイコ
  後処理:
    - Rule().成立役一覧.append((Rule().点数, '剛田家セット'))

未来セット:
  構造: 3 3 3
  前処理:
    - Rule().点数 = 300
  制約:
    - and
    -
      - 同色(a, b, c)
      - a.色 == '赤'
      - 同色(d, e, f)
      - d.色 == '橙'
      - g == h == i == セワシ
  後処理:
    - Rule().成立役一覧.append((Rule().点数, '未来セット'))

ジャイアンリサイタル:
  構造: 3 3 3
  前処理:
    - Rule().点数 = 240
  制約:
    - and
    -
      - a == b == c == 歌うジャイアン
      - 同色(d, e, f)
      - d.色 != '水'
      - 同色(g, h, i)
      - g.色 != '水'
  後処理:
    - Rule().成立役一覧.append((Rule().点数, 'ジャイアンリサイタル'))

なかよしセット:
  構造: 3 3 3
  前処理:
    - Rule().点数 = 240
  制約:
    - and
    -
      - 同色(a, b, c)
      - a.色 == '赤'
      - 同色(d, e, f)
      - d.色 == '黄'
      - 同色(g, h, i)
  後処理:
    - Rule().成立役一覧.append((Rule().点数, 'なかよしセット'))

大好きセット:
  構造: 3 3 3
  前処理:
    - Rule().点数 = 240
  制約:
    - and
    -
      - 同色(a, b, c)
      - a.色 == '黄'
      - 同色(d, e, f)
      - d.色 == '桃'
      - 同色(g, h, i)
  後処理:
    - Rule().成立役一覧.append((Rule().点数, '大好きセット'))

お前の物は俺の物俺の物は俺の物セット:
  構造: 3 3 3
  前処理:
    - Rule().点数 = 180
  制約:
    - and
    -
      - 同色(a, b, c)
      - a.色 == '水'
      - 同色(d, e, f)
      - d.色 == '緑'
      - 同色(g, h, i)
  後処理:
    - Rule().成立役一覧.append((Rule().点数, 'お前の物は俺の物俺の物は俺の物セット'))
